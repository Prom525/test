[pytest]
testpaths = tests
markers =
    integration: requires explicitly configured live services
    known_gap: documents a known baseline gap without failing the characterization suite
    p4_14c: existing P4.14c contract tests

